<div class="pages">
    <b class="page-selector">
        <a class="page-selector-a {{"mouse-disabled" if pdisable else ""}}" href="{{plink}}"><</a>
    </b>
    <p>{{pagestart}}-{{pageend}} {{page}}/{{totalpages}}</p>
    <b class="page-selector">
        <a class="page-selector-a {{"mouse-disabled" if ndisable else ""}}" href="{{nlink}}">></a>
    </b>
</div>